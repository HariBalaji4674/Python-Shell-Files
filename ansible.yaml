- name: Insatlling 
  host: localhost
  become: yes
  vars:
    name: peddireddy
  tasks:
  - name: install nginx
    yum:
      name: nginx
      stage: present
  - name: Checking the varibales
    debug:
      msg: "my anme is {{name}}"

1. Shared Libraries
2. GitLab CI&CD 
3. 